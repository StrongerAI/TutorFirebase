
"use client";

// This component is no longer used as navigation has moved to the AppHeader.
// It's kept here to avoid breaking imports if any, but should be removed in a future cleanup.
export function AppSidebar() {
  return null;
}
